[phases.install]
cmds = ["pnpm install --no-frozen-lockfile --ignore-scripts && pnpm rebuild @parcel/watcher esbuild"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "node .output/server/index.mjs"
