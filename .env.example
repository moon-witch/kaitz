# Directus CMS base URL (public — used client-side and server-side)
DIRECTUS_URL=https://your-directus-instance.example.com

# Directus admin token (private — server-side only, never exposed to browser)
DIRECTUS_ADMIN_TOKEN=your-admin-token-here
